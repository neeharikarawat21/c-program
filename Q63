/*Q63: Merge two arrays.
Sample Test Cases:
Input 1:
3
1 2 3
2
4 5
Output 1:
1 2 3 4 5
*/
#include <stdio.h>
int main()
{
    int arr1[50],arr2[50],n1,n2,i;
    printf("How many elements do you want to enter in the first array=");
    scanf("%d",&n1);
    printf("Enter the elements=");
    for(i=0;i<n1;i++)
    {
   scanf("%d",&arr1[i]);
    }
    printf("How many elements do you want to enter in the second array=");
    scanf("%d",&n2);
    printf("Enter the elements=");
    for(i=0;i<n2;i++)
    {
   scanf("%d",&arr2[i]);
    }
    for(i=0;i<n1;i++)
    {
        array[i]=arr1[i];
    }
    for(i=0;i<n2;i++)
    {
        arr1[n1+i]=arr2[i];
    }
    printf("Merged array=");
    for(i=0;i<n1+n2;i++)
    {
    printf("%d ",arr1[i]);
    }
    return 0;
}