 //Write a program to find sum of digits of two number
#include<stdio.h>
int main()
{
    int num,digit,sum=0;
    printf("Enter the number=");
    scanf("%d",&num);
    while(num>0)
    {
     digit=num%10;
     sum=sum+digit;
     num=num/10;
    }
    printf("Sum=%d",sum);
}